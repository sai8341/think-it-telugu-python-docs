---
title: "What are Tuples? - Python in Telugu | Think IT Telugu"
sidebar_label: "What are Tuples?"
description: "Learn what Tuples are in Python with simple real-world comparisons and easy examples."
keywords: ["Python", "What are Tuples?", "Telugu tutorial", "Learn Python in Telugu", "AI", "Data Science"]
---

# What are Tuples? (Locked Lists)

A **Tuple** is almost identical to a **List**, with just one simple rule: **Once created, a Tuple cannot be changed!**

- A **List** uses square brackets **`[]`** &rarr; Items can be added, removed, or changed.
- A **Tuple** uses parentheses **`()`** &rarr; Items are locked and permanent.

---

## 1. Real-Life Metaphor: Open Box vs Locked Box

Think of data containers in real life:

- **List `[ ]` = An Open Storage Box**  
  You can put items in, take items out, or replace an item with a new one whenever you want (like a Shopping Cart).

- **Tuple `( )` = A Sealed Glass Case**  
  Once items are placed inside, they are locked forever. Nobody can alter, add, or delete any item (like your Date of Birth or Aadhar Card Number).

{/* Visual Comparison Card: List [] vs Tuple () */}
<div style={{ background: '#f8fafc', border: '1px solid #cbd5e1', borderRadius: '10px', padding: '16px', margin: '20px 0' }}>

  <p style={{ fontFamily: 'sans-serif', fontSize: '12px', fontWeight: '800', color: '#64748b', marginTop: 0, marginBottom: '12px', textTransform: 'uppercase', letterSpacing: '0.05em' }}>
    Quick Comparison: List vs Tuple
  </p>

  <div style={{ display: 'flex', gap: '12px', flexWrap: 'wrap' }}>
    
    {/* Left Box: List */}
    <div style={{ flex: 1, minWidth: '220px', background: '#e0f2fe', border: '2px solid #0284c7', borderRadius: '8px', padding: '12px 14px' }}>
      <span style={{ fontSize: '11px', fontWeight: '800', color: '#0369a1', textTransform: 'uppercase' }}>LIST [ ] (CHANGEABLE)</span>
      <pre style={{ background: '#ffffff', padding: '8px', borderRadius: '4px', fontSize: '12px', marginTop: '6px', color: '#0c4a6e', marginBottom: 0 }}>
{`fruits = ["apple", "banana"]
fruits[0] = "mango" # Allowed!`}
      </pre>
    </div>

    {/* Right Box: Tuple */}
    <div style={{ flex: 1, minWidth: '220px', background: '#f3e8ff', border: '2px solid #a855f7', borderRadius: '8px', padding: '12px 14px' }}>
      <span style={{ fontSize: '11px', fontWeight: '800', color: '#7e22ce', textTransform: 'uppercase' }}>TUPLE ( ) (LOCKED)</span>
      <pre style={{ background: '#ffffff', padding: '8px', borderRadius: '4px', fontSize: '12px', marginTop: '6px', color: '#6b21a8', marginBottom: 0 }}>
{`fruits = ("apple", "banana")
fruits[0] = "mango" # Error! Locked`}
      </pre>
    </div>

  </div>

</div>

---

## 2. Creating and Reading Tuples

Creating a tuple is as simple as using round brackets `()`:

```python
# A tuple storing fixed screen resolution (width, height)
resolution = (1920, 1080)

# A tuple storing a date of birth (day, month, year)
dob = (25, "December", 2000)

# Reading items works starting at index 0 (just like lists!)
print(dob[0])  # Output: 25
print(dob[1])  # Output: December
```

---

## 3. Why Do We Need Tuples?

Beginners often ask: **"If Lists can do everything, why do we need Tuples?"**

1. **Data Safety:** Important data (like User ID, Coordinates, or Passwords) won't be accidentally modified or deleted by mistake.
2. **Faster Performance:** Python processes Tuples faster than Lists because their size is fixed.

---

## 4. Single-Item Tuple Tip

If you want to create a tuple with **only 1 item**, you must add a comma `,` after the item:

```python
# Without comma: Python thinks it is a normal number
my_num = (5)
print(type(my_num))  # Output: <class 'int'>

# With comma: Python knows it is a Tuple!
my_tuple = (5,)
print(type(my_tuple))  # Output: <class 'tuple'>
```

---

## Summary

*   **List `[ ]`:** Use when items will change, grow, or shrink over time.
*   **Tuple `( )`:** Use when items are fixed and must never be changed.
*   Tuples use **round brackets `()`** and index positions starting from **`0`**.

---

## What's Next?

Next, let's learn how to assign multiple tuple values to multiple variables at once using **Packing & Unpacking**!