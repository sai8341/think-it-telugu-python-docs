---
title: "JSON Structure - Python in Telugu | Think IT Telugu"
sidebar_label: "JSON Structure"
description: "Learn about JSON Structure in Python with real-world examples. This Think IT Telugu tutorial explains JSON Structure easily for AI and Data Science beginners."
keywords: ["Python", "JSON Structure", "Telugu tutorial", "Learn Python in Telugu", "AI", "Data Science"]
---

# JSON Structure

JSON data is structured as key-value pairs, which looks very similar to a Python dictionary.

```json
{
  "name": "Sai Kumar",
  "age": 22,
  "skills": ["Python", "JavaScript"],
  "is_student": false
}
```
### Differences (Python Dictionary vs JSON):
1. In JSON, keys must always use double quotes (`"..."`). Single quotes are invalid.
2. Boolean values in JSON are lowercase (`true`, `false`). In Python, they are capitalized (`True`, `False`).
3. JSON uses `null` to represent nothing, which maps to `None` in Python.

---
